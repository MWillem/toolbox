[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cyber-learning-toolbox"
version = "2.13.0"
description = "Toolbox guidée pour apprendre les bases d'un audit de cybersécurité."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Maréchaux Willem"}]
dependencies = ["qrcode[pil]>=7.4"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: Security",
]

[project.scripts]
cybertoolbox = "cybertoolbox.cli:main"

[tool.setuptools.packages.find]
include = ["cybertoolbox*"]
